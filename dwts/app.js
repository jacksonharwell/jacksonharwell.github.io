const fmt=n=>Number.isFinite(+n)?+n:0;let DATA;fetch('league-data.json',{cache:'no-store'}).then(r=>r.json()).then(d=>{DATA=d;render()}).catch(()=>document.querySelector('main').innerHTML='<div class="empty">League data could not be loaded.</div>');const activateTab=id=>{const target=document.getElementById(id)||document.getElementById('league');document.querySelectorAll('.tab,.panel').forEach(x=>x.classList.remove('active'));target.classList.add('active');const button=document.querySelector(`.tab[data-tab="${target.id}"]`);if(button)button.classList.add('active')};document.querySelectorAll('.tab').forEach(b=>b.onclick=()=>{activateTab(b.dataset.tab);history.replaceState(null,'',`#${b.dataset.tab}`)});activateTab(location.hash.slice(1)||'league');function render(){const{league,owners,dancers,transactions,weeks}=DATA;document.getElementById('weekChip').textContent=league.week?`Week ${league.week} • ${league.phase}`:league.phase;document.getElementById('draftDate').textContent=`${league.draftDate.replace(', 2026','')} • ${league.draftTime}`;document.getElementById('updatedText').textContent=`Updated ${league.lastUpdated}`;const sorted=[...owners].sort((a,b)=>fmt(b.points)-fmt(a.points)||a.name.localeCompare(b.name));const lead=sorted[0];document.getElementById('leaderName').textContent=fmt(lead.points)>0?lead.name:'Draft Night Awaits';document.getElementById('leaderScore').textContent=fmt(lead.points);document.getElementById('leaderMeta').textContent=fmt(lead.points)>0?`${lead.bux} Dancer Bux remaining`:`${owners.length} owners • 200 Dancer Bux each`;document.getElementById('standings').innerHTML=sorted.map((o,i)=>standing(o,i,dancers)).join('');document.getElementById('pulse').innerHTML=pulse(sorted,weeks);document.getElementById('dancerCards').innerHTML=dancers.length?dancers.map(profileCard).join(''):'<div class="empty">Profiles will appear as contestants are announced.</div>';const fa=dancers.filter(d=>!d.ownerId&&d.status!=='eliminated'&&DATA.league.week>0);document.getElementById('marketCards').innerHTML=fa.length?fa.map(marketCard).join(''):'<div class="empty">The waiver market opens after the auction draft. Until then, use the Study tab to scout the cast.</div>';document.getElementById('transactions').innerHTML=transactions.length?[...transactions].reverse().map(t=>`<div class="event"><strong>${t.title}</strong><span>${t.detail}</span><br><time>${t.date}</time></div>`).join(''):'<div class="empty">Auction purchases, trades, waiver claims and cuts will be logged here.</div>'}function standing(o,i,dancers){const roster=dancers.filter(d=>d.ownerId===o.id).map(d=>d.name).join(' • ')||'No dancers yet';const m=fmt(o.movement);return `<div class="standing"><div class="rank">${i+1}</div><div><span class="name">${o.name}</span><span class="sub">${roster} • ${o.bux} Bux</span></div><div class="points">${fmt(o.points)}</div><div class="move ${m>0?'up':m<0?'down':'flat'}">${m>0?'▲ '+m:m<0?'▼ '+Math.abs(m):'—'}</div></div>`}function profileCard(d){const pro=`<div class="pro-chip">${d.proImage?`<img class="pro-headshot" src="${d.proImage}" alt="${d.pro}" loading="lazy" onerror="this.style.display='none'">`:''}<span><small>PRO</small>${d.pro||'TBD'}</span></div>`;const source=d.source?`<a class="profile-source" href="${d.source}" target="_blank" rel="noopener">Profile source ↗</a>`:'';return `<article class="profile-card"><div class="portrait-stage"><img src="${d.image}" alt="${d.name}" loading="lazy" onerror="this.style.display='none'"><span class="confirmed">OFFICIAL PAIRING</span></div><div class="profile-copy"><div class="profile-title"><div><h3>${d.name}</h3><span>${d.knownFor}</span></div>${pro}</div><div class="scout-line"><strong>Dance read:</strong> ${d.danceBackground}</div><p>${d.bio}</p>${source}<div class="preseason-row"><span>Auction cost</span><b>${d.auctionCost??'TBD'}</b><span>Owner</span><b>${d.ownerName||'Undrafted'}</b></div></div></article>`}function marketCard(d){const ask=marketGuide(d);return `<article class="card"><span class="eyebrow">FREE AGENT</span><h3>${d.name}</h3><p>${d.pro||'Pro TBD'} • ${d.avgScore??'—'} avg pts</p><div class="market-price">${ask}</div><span class="sub">suggested discussion range</span></article>`}function marketGuide(d){const owners=DATA.owners,med=[...owners].map(o=>fmt(o.bux)).sort((a,b)=>a-b)[Math.floor(owners.length/2)]||200;let mid=Math.max(1,Math.round((fmt(d.avgScore)/10)*med*.28+Math.max(-5,Math.min(8,fmt(d.trend)*2))));return `${Math.max(1,mid-6)}–${mid+6} Bux`}function pulse(sorted,weeks){if(!weeks.length)return '<div class="mini-row"><span>Auction Draft</span><strong>Sept. 8 • 7 PM</strong></div><div class="mini-row"><span>Premiere</span><strong>Sept. 15</strong></div><div class="mini-row"><span>Shakeup</span><strong>after Week 6</strong></div>';const biggest=[...sorted].sort((a,b)=>fmt(b.movement)-fmt(a.movement))[0];return `<div class="mini-row"><span>Biggest riser</span><strong>${biggest.name}</strong></div><div class="mini-row"><span>Week completed</span><strong>${DATA.league.week}</strong></div>`}