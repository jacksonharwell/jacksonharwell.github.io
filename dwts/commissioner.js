const NOTES_KEY='dwts-commissioner-notes-v1',WHEEL_KEY='dwts-draft-wheel-removed-v1',AUCTION_KEY='dwts-auction-state-v1',LOCK_KEY='dwts-weekly-locks-v1',TRADE_KEY='dwts-trade-status-v1';const el=id=>document.getElementById(id);function notes(){try{return JSON.parse(localStorage.getItem(NOTES_KEY)||'[]')}catch{return[]}}function esc(s){return String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}function renderNotes(){const all=notes();el('notesList').innerHTML=all.length?all.slice().reverse().map(n=>'<article class="note-item"><p>'+esc(n.text)+'</p><footer><span>'+(n.visibility==='public'?'Public update':'Private note')+' • '+new Date(n.createdAt).toLocaleString()+'</span><button data-delete="'+n.id+'" type="button">Delete</button></footer></article>').join(''):'<div class="empty">No commissioner notes yet.</div>';document.querySelectorAll('[data-delete]').forEach(b=>b.onclick=()=>deleteNote(b.dataset.delete))}function addNote(){const text=el('noteText').value.trim();if(!text){el('noteStatus').textContent='Write a note first.';return}const all=notes();all.push({id:String(Date.now()),text,visibility:el('noteVisibility').value,createdAt:new Date().toISOString()});localStorage.setItem(NOTES_KEY,JSON.stringify(all));el('noteText').value='';el('noteStatus').textContent=el('noteVisibility').value==='public'?'Saved and added to this device’s Dashboard.':'Private note saved.';renderNotes()}function deleteNote(id){localStorage.setItem(NOTES_KEY,JSON.stringify(notes().filter(n=>n.id!==id)));renderNotes()}function reset(kind){const messages={wheel:'Restore all couples to the wheel? Existing buyers and bids will remain recorded.',draft:'Erase the entire draft on this device—including buyers, bids, waivers and wheel progress?',locks:'Clear every current Lock of the Week?',labels:'Clear every dancer trade-status label?'};if(!confirm(messages[kind]))return;if(kind==='wheel')localStorage.removeItem(WHEEL_KEY);if(kind==='draft'){localStorage.removeItem(WHEEL_KEY);localStorage.removeItem(AUCTION_KEY)}if(kind==='locks')localStorage.removeItem(LOCK_KEY);if(kind==='labels')localStorage.removeItem(TRADE_KEY);el('resetStatus').textContent='Reset complete.'}el('addNote').onclick=addNote;document.querySelectorAll('[data-reset]').forEach(b=>b.onclick=()=>reset(b.dataset.reset));renderNotes();